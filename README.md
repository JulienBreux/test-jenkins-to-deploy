# Test Jenkins To Deploy

## Install

To install dependencies, run this following command:
```sh
bundle install
```

## Tests

To run tests and coverage repport, run this following command:
```sh
bundle exec rake spec
```

